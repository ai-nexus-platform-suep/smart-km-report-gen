"""上下文管理：历史消息拼装 + Token 截断 (人员 C 独占)"""


def build_context(messages: list, max_tokens: int = 8192) -> list[dict]:
    pass


def truncate_by_tokens(messages: list, max_tokens: int) -> list:
    pass
