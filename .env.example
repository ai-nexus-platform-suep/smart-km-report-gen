# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=km_db
MYSQL_USER=km
MYSQL_PASSWORD=km123456

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=km
RABBITMQ_PASSWORD=km123456

# MinIO
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=km-documents

# Milvus（向量库，大组统一选型）
MILVUS_HOST=localhost
MILVUS_PORT=19530

# km-ai-service（Python：chunk / embed / rerank）
KM_AI_SERVICE_URL=http://localhost:8092

# km-ingest-service（Python：MinIO upload / MinerU parse）
KM_INGEST_SERVICE_URL=http://localhost:8093
KM_INGEST_SERVICE_WORK_DIR=km-ingest-service/.work
MINERU_BACKEND=pipeline
MINERU_MODEL_SOURCE=modelscope
PIP_CACHE_DIR=km-ingest-service/.cache/pip
TEMP=km-ingest-service/.tmp
TMP=km-ingest-service/.tmp
HF_HOME=km-ingest-service/.cache/huggingface
HUGGINGFACE_HUB_CACHE=km-ingest-service/.cache/huggingface/hub
MODELSCOPE_CACHE=km-ingest-service/.cache/modelscope
MINERU_TOOLS_CONFIG_JSON=km-ingest-service/.mineru/mineru.json

# JWT
JWT_SECRET=change-me-in-production-km-platform-2026
JWT_EXPIRE_HOURS=24

# SiliconFlow（由 km-ai-service 使用）
SILICONFLOW_API_KEY=
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
