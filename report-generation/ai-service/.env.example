# LLM API（OpenAI 兼容，如 DeepSeek / 通义 / 本地 Ollama）
LLM_API_URL=your-api-url
LLM_API_KEY=your-api-key
LLM_MODEL=your-model
LLM_JSON_MODE=true

# 超时（秒）
LLM_TIMEOUT_SECONDS=180

# true = 不调用真实 LLM，返回本地模板（联调 SSE 通道用）
LLM_MOCK=false

# 服务端口（默认 9000，与 nacos app.ai.outline-url 一致）
AI_SERVICE_PORT=9000
