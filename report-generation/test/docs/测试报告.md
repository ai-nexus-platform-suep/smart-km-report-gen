# 测试报告

> 项目：技术监督辅助平台 · 报告生成模块（feat-c）  
> 测试范围：`report-common`、`report-outline-docx`、报告生成核心接口  
> 日期：2026-06-29

---

## 1. 测试结论

| 项目 | 结论 |
|---|---|
| 自动化测试 | 通过 |
| 通过用例数 | 15 |
| 失败用例数 | 0 |
| 错误用例数 | 0 |
| 本次覆盖范围 | 健康检查、固定大纲、AI 大纲兜底、确认保存边界、DOCX 导出、文件下载 |
| 未覆盖范围 | 认证 Gateway、SSE、章节内容、历史记录、管理后台配置与统计、模板管理 |

总体结论：

后端 1 已实现范围的核心逻辑通过自动化测试。后端 2 标注为待开发的接口尚未执行验收测试，需等接口完成后补充。

---

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 操作系统 | Windows 11 |
| JDK | 17.0.17 |
| Maven | `C:\apache-maven-3.8.8\bin\mvn.cmd` |
| 默认 Maven | `C:\mavennew\apache-maven-3.6.2`，版本过低，不建议使用 |
| 测试框架 | JUnit 5、Mockito、Spring MockMvc |
| 测试模块 | `report-outline-docx` |

执行命令：

```powershell
cd C:\Users\hp\Desktop\课设\smart-km-report-gen
& 'C:\apache-maven-3.8.8\bin\mvn.cmd' -q -pl report-generation/report-outline-docx test
```

---

## 3. 测试文件

| 文件 | 用例数 | 覆盖功能 |
|---|---:|---|
| `report-generation/test/java/com/powerreport/controller/ReportControllerTest.java` | 7 | Controller 接口层、请求校验、响应结构、下载响应头 |
| `report-generation/test/java/com/powerreport/service/OutlineServiceImplTest.java` | 5 | 大纲模板、AI 兜底、确认保存边界 |
| `report-generation/test/java/com/powerreport/service/DocxExportServiceImplTest.java` | 3 | DOCX 导出、文件记录、异常处理 |

说明：

- 测试代码目前集中放在 `report-generation/test/java`。
- 为满足“只提交 test 文件夹”的要求，没有修改业务模块 POM。
- 如需重新运行这些测试，需要临时复制到 `report-outline-docx/src/test/java`，或配置测试源码目录。

---

## 4. 自动化测试结果

| 测试类 | 用例数 | 失败 | 错误 | 跳过 | 结果 |
|---|---:|---:|---:|---:|---|
| `ReportControllerTest` | 7 | 0 | 0 | 0 | 通过 |
| `OutlineServiceImplTest` | 5 | 0 | 0 | 0 | 通过 |
| `DocxExportServiceImplTest` | 3 | 0 | 0 | 0 | 通过 |
| 合计 | 15 | 0 | 0 | 0 | 通过 |

---

## 5. 接口覆盖情况

### 5.1 已覆盖接口

| 接口 | 覆盖方式 | 覆盖内容 | 结果 |
|---|---|---|---|
| `GET /api/health` | MockMvc | 健康检查响应 | 通过 |
| `POST /api/reports/outline` | MockMvc + Service | 固定大纲成功、缺少 `reportType` | 通过 |
| `POST /api/reports/outline/generate` | MockMvc + Service | `subject` 为空校验、AI 未配置本地兜底、关闭兜底异常 | 通过 |
| `POST /api/reports/outline/confirm` | MockMvc + Service | 空大纲且无 `tempId` 边界 | 通过 |
| `POST /api/reports/{reportId}/export/docx` | MockMvc + Service | 默认参数、DOCX 生成、报告不存在 | 通过 |
| `GET /api/reports/files/{fileId}/download` | MockMvc + Service | 下载响应头、文件流、文件记录不存在 | 通过 |

### 5.2 未覆盖接口

| 模块 | 接口 | 原因 |
|---|---|---|
| 认证 Gateway | `/api/auth/*` | 不属于 `report-common` 和 `report-outline-docx` 当前源码范围 |
| SSE 流式生成 | 接口 11-12 | 后端 2 待开发 |
| 章节内容管理 | 接口 13-16 | 后端 2 待开发 |
| 历史记录管理 | 接口 17-19 | 后端 2 待开发 |
| 管理后台统计概览 | 接口 20-21 | 后端 2 待开发 |
| 管理后台模型配置 | 接口 22-23 | 后端 2 待开发 |
| 管理后台模板管理 | 接口 24-26 | 后端 2 待开发 |

---

## 6. 测试用例明细

### 6.1 ReportControllerTest

| 用例 | 检测内容 | 结果 |
|---|---|---|
| `healthReturnsServiceScope` | `GET /api/health` 返回服务状态和范围 | 通过 |
| `outlineReturnsFixedTemplate` | 固定大纲接口返回大纲节点 | 通过 |
| `outlineRejectsMissingReportType` | `reportType` 缺失返回 400 | 通过 |
| `generateOutlineRejectsBlankSubject` | `subject` 空白返回 400 | 通过 |
| `confirmOutlineMapsIllegalArgumentToBadRequest` | 确认保存业务异常映射为 400 | 通过 |
| `exportDocxUsesDefaultRequestWhenBodyIsMissing` | 导出接口无请求体时使用默认参数 | 通过 |
| `downloadFileReturnsDocxStream` | 下载接口返回 DOCX Content-Type 和文件流 | 通过 |

### 6.2 OutlineServiceImplTest

| 用例 | 检测内容 | 结果 |
|---|---|---|
| `buildSummerPeakOutlineReturnsNormalizedTemplate` | 迎峰度夏模板结构正确 | 通过 |
| `buildCoalInventoryOutlineReturnsAuditTemplate` | 煤库存审计模板结构正确 | 通过 |
| `generateOutlineFallsBackToLocalTemplateWhenAiUrlIsMissing` | AI 地址未配置时使用本地模板兜底 | 通过 |
| `generateOutlineThrowsWhenFallbackIsDisabledAndAiUrlIsMissing` | 关闭兜底时 AI 未配置会报错 | 通过 |
| `confirmOutlineRejectsEmptyOutlineWithoutTempId` | 空大纲且无 `tempId` 时拒绝保存 | 通过 |

### 6.3 DocxExportServiceImplTest

| 用例 | 检测内容 | 结果 |
|---|---|---|
| `exportReportWritesDocxAndPersistsFileRecord` | 生成 DOCX、计算 SHA256、保存文件记录 | 通过 |
| `exportReportRejectsMissingReport` | 报告不存在时报错 | 通过 |
| `getFileForDownloadRejectsMissingFileRecord` | 文件记录不存在时报错 | 通过 |

---

## 7. 功能验收情况

| 验收项 | 对应接口/功能 | 结果 | 说明 |
|---|---|---|---|
| 健康检查可用 | `GET /api/health` | 通过 | 返回 `status=ok` |
| 固定大纲可返回 | `POST /api/reports/outline` | 通过 | 支持两类报告模板 |
| 参数校验有效 | 大纲、生成、确认接口 | 通过 | 缺少必填字段返回 400 |
| AI 不可用时可兜底 | `generateOutline` | 通过 | `fallback-enabled=true` 时返回本地模板 |
| 关闭兜底后能暴露错误 | `generateOutline` | 通过 | `fallback-enabled=false` 时抛出异常 |
| 空大纲保护 | `confirmOutline` | 通过 | 无 `tempId` 时拒绝保存 |
| DOCX 可生成 | `exportReport` | 通过 | 生成 `.docx` 文件并保存记录 |
| 下载响应正确 | `downloadFile` | 通过 | 返回 DOCX 文件流和下载头 |

---

## 8. 边界条件覆盖

| 边界场景 | 覆盖情况 | 结果 |
|---|---|---|
| `reportType` 缺失 | 已覆盖 | 通过 |
| `subject` 空白 | 已覆盖 | 通过 |
| `outline` 为空且无 `tempId` | 已覆盖 | 通过 |
| AI URL 未配置 | 已覆盖 | 通过 |
| AI URL 未配置且关闭兜底 | 已覆盖 | 通过 |
| 导出请求体为空 | 已覆盖 | 通过 |
| 报告不存在 | 已覆盖 | 通过 |
| 文件记录不存在 | 已覆盖 | 通过 |
| 文件下载响应头 | 已覆盖 | 通过 |

---

## 9. 遗留风险

| 风险 | 说明 | 建议 |
|---|---|---|
| 真实数据库链路未在本报告中执行 | 当前自动化使用 Mock Mapper，不连接 MySQL | 配置 MySQL、Nacos 后使用 Apifox 做真实接口联调 |
| Redis 真实写入未在本报告中执行 | 当前使用 Mock `StringRedisTemplate` | 联调环境验证 `tempId` 写入、读取、过期 |
| AI 真实服务未调用 | 当前只验证本地兜底和关闭兜底错误 | AI 服务可用后设置 `fallback-enabled=false` 验证真实调用 |
| 后端 2 接口未验收 | SSE、历史、配置、统计、模板仍为待开发 | 后端 2 完成后补充用例和报告 |
| 测试代码集中目录不是 Maven 默认目录 | 为满足只提交 `test` 目录的要求未修改 POM | 复跑测试时临时复制或配置测试源码目录 |

---

## 10. Bug 清单

本次自动化测试未发现后端 1 已覆盖范围内的功能缺陷。

| Bug ID | 问题 | 优先级 | 状态 |
|---|---|---|---|
| 无 | 本次未发现 | - | - |

---

## 11. 附录：建议 Apifox 截图清单

后续真实环境联调时建议补充以下截图：

| 截图编号 | 接口 | 场景 |
|---|---|---|
| IMG-001 | `GET /api/health` | 健康检查成功 |
| IMG-002 | `POST /api/reports/outline` | 固定大纲成功 |
| IMG-003 | `POST /api/reports/outline/generate` | AI 大纲或本地兜底成功 |
| IMG-004 | `POST /api/reports/outline/confirm` | 确认保存成功 |
| IMG-005 | `POST /api/reports/{reportId}/export/docx` | DOCX 导出成功 |
| IMG-006 | `GET /api/reports/files/{fileId}/download` | DOCX 下载成功 |
| IMG-007 | 参数异常接口 | 返回 400 |

