# B 组真实联调时保持空值，交给 Vite proxy 转发到 qa-agent / Spring Boot。
VITE_API_BASE=
VITE_ENABLE_MOCK=true
# qa-real 模式建议只保留登录、知识库、报告 mock，避免拦截真实 QA 接口。
VITE_MOCK_MODULES=auth,km,report
VITE_QA_AGENT_TARGET=http://localhost:8000
VITE_QA_SERVICE_TARGET=http://localhost:8082
