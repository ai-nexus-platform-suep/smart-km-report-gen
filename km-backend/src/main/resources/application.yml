spring:
  application:
    name: km-backend
  profiles:
    active: h2

server:
  port: 8091

management:
  endpoints:
    web:
      exposure:
        include: health,info

km:
  ai-service:
    base-url: http://localhost:8092
