<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.km.repository.ChunkMapper">

    <resultMap id="BaseResultMap" type="com.km.entity.Chunk">
        <id column="id" property="id"/>
        <result column="doc_id" property="docId"/>
        <result column="content" property="content"/>
        <result column="chapter_path" property="chapterPath"/>
        <result column="chunk_index" property="chunkIndex"/>
        <result column="chunk_type" property="chunkType"/>
        <result column="vector_id" property="vectorId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, doc_id, content, chapter_path, chunk_index, chunk_type, vector_id, created_at
    </sql>

    <insert id="insert" parameterType="com.km.entity.Chunk">
        INSERT INTO chunk (id, doc_id, content, chapter_path, chunk_index, chunk_type, vector_id, created_at)
        VALUES (#{id}, #{docId}, #{content}, #{chapterPath}, #{chunkIndex}, #{chunkType}, #{vectorId}, NOW())
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO chunk (id, doc_id, content, chapter_path, chunk_index, chunk_type, vector_id, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.docId}, #{item.content}, #{item.chapterPath},
             #{item.chunkIndex}, #{item.chunkType}, #{item.vectorId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteByDocId">
        DELETE FROM chunk WHERE doc_id = #{docId}
    </delete>

    <delete id="deleteByDocIds">
        DELETE FROM chunk WHERE doc_id IN
        <foreach collection="docIds" item="docId" open="(" separator="," close=")">
            #{docId}
        </foreach>
    </delete>

    <select id="listByDocId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM chunk
        WHERE doc_id = #{docId}
        ORDER BY chunk_index ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByDocId" resultType="long">
        SELECT COUNT(*)
        FROM chunk
        WHERE doc_id = #{docId}
    </select>

    <!-- AI 服务降级时使用的关键词匹配搜索（已废弃） -->
    <select id="searchByKeyword" resultMap="BaseResultMap">
        SELECT c.id, c.doc_id, c.content, c.chapter_path, c.chunk_index, c.chunk_type, c.vector_id, c.created_at
        FROM chunk c
        INNER JOIN document d ON d.id = c.doc_id
        WHERE c.content LIKE CONCAT('%', #{keyword}, '%')
        <if test="kbIds != null and kbIds.size() &gt; 0">
            AND d.kb_id IN
            <foreach collection="kbIds" item="kbId" open="(" separator="," close=")">
                #{kbId}
            </foreach>
        </if>
        ORDER BY c.chunk_index ASC
        LIMIT #{limit}
    </select>

    <!-- BM25 降级搜索：按关键词搜索指定文档列表中的 chunk -->
    <!-- 对应 EPIC-05 05.5 检索降级策略统一 -->
    <select id="searchByKeywordWithDocIds" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM chunk
        WHERE doc_id IN
        <foreach collection="docIds" item="docId" open="(" separator="," close=")">
            #{docId}
        </foreach>
        AND content LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY chunk_index ASC
        LIMIT #{limit}
    </select>

</mapper>
