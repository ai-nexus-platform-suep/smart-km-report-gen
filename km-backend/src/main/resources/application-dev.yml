spring:
  datasource:
    url: jdbc:mysql://localhost:3306/km_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

  redis:
    host: localhost
    port: 6379

jwt:
  secret: km-platform-dev-secret-key-do-not-use-in-prod
  expiration: 86400000

logging:
  level:
    com.km: DEBUG
