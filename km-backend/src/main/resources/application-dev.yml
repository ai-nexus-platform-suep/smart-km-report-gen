spring:
  datasource:
    url: jdbc:mysql://localhost:3306/km_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: km
    password: km123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: km
    password: km123456
    virtual-host: /
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

km:
  minio:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: km-documents
  ai-service:
    base-url: http://localhost:8092
    callback-url: http://localhost:8091/internal/document/status
  jwt:
    secret: change-me-in-production-km-platform-2026
    expire-hours: 24
